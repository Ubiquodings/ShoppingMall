/*
var cartListQuantity = {

    var sell_price;
    var amount;

    init: function () {

        let _this = this;

        sell_price = $('.sell_price').val();
        amount = $('.amount').val();
        $('.sum').val(sell_price);

        //let sum = $('.sum').val();
        //sum = sell_price;
        $.fn.change();
    },
    $.fn.del = function () {
    var hm =  $('.amount').value;
    var sum = $('.sum').value;

    if(hm > 1) {
        hm -- ;
        sum = parseInt(hm) * sell_price;
    }

    }

};

cartListQuantity.init();*/
