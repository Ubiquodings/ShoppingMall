{{>layout/header}}
<style>

    .img-responsive {
        width: 100%;
    }
</style>

<div class="card">
    <div class="card-body">
        - 여기는 결재 페이지
    </div>
</div>


{{#allPayments}} <!--List<Order>-->

<input type="hidden" value="{{id}}" id="input-order-id"/> <!-- Order 의 id -->

<div class="row" id="order-list-row">
    <div class="card" style="width: 100%;">
        <div class="card-body">
            <div class="row">
                <div class="col-lg-2">
                    <img src="{{product.imgUrl}}" class="img-responsive"/>
                </div>
                <div class="col-lg-10">
                    <h5 class="card-title">{{product.name}}</h5> <!--잘나온다!-->
                    <h5 class="card-title">상품 원가 : {{product.price}}</h5>
                    <h5 class="card-title">개수 : {{count}}</h5>
                    <!--            <button type="button" class="btn btn-primary btn-lg" id="btn-cart-order">주문</button>-->
                    <input type="hidden" value="{{product.id}}" id="input-cart-order"/>
                    <!--                    <a href="/orders/new/{{product.id}}" class="btn btn-info active" role="button">주문</a>-->
                </div>
            </div>
        </div>

        <div>
            <div class="main2">
                <div id="main21">
                    <!--                    <button type="button" class="btn btn-primary btn-lg" id="btn-order-cancel">환불 요청(주문취소)</button>-->

                    <!--                    <button> 환불 요청(주문취소) </button>-->
                    <!--                    <button> 교환 요청 </button>-->
                    <!--                    <button> 리뷰 쓰기 </button>-->
                </div>
            </div>
        </div>


    </div>
</div>
{{/allPayments}}

<div class="card">
    <div class="card-body">
        - 가져온 상품 관련 쿠폰을 가져옵니다
    </div>
</div>

{{#couponList}} <!--List<Coupon>-->
<div class="row">
    <div class="card" style="width: 100%;">
        <input type="hidden" value="{{id}}" class="input-coupon-id"/>
        <div class="card-body">
            <div class="px-4 py-3">
                <input type="checkbox" class="form-check-input" id="">
                <h5 class="card-title">{{id}}</h5> <!--id 확인테스트-->
                <h5 class="card-title">{{name}}</h5>
            </div>
        </div>
    </div>
</div>
{{/couponList}}


<div class="card">
    <div class="card-body">
        - 최종 주문 버튼을 누르면, 장바구니의 모든 아이템이 삭제되고 주문처리됩니다
    </div>
</div>

<button type="button" class="btn btn-primary btn-lg btn-order-all btn-block btn-order-at-payment">주문</button>
{{>layout/footer}}