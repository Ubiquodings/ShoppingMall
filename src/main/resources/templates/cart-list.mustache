{{>layout/header}}
cart-list
{{#shopLists}} <!--List<ShopList>-->
        <div class="row">
            <div class="card" style="width: 100%;">
                <div class="card-body">
                    <h5 class="card-title">{{product.name}}</h5> <!--잘나온다!-->
                    <h5 class="card-title" id="cart-item-count">{{count}}</h5>
                    <button type="button" class="btn btn-primary btn-lg btn-order-immediately-from-cart"><input type="hidden" id="cart-item-id" value="{{id}}"/><input type="hidden" id="cart-item-count" value="{{count}}"/>주문</button>
                    <input type="hidden" value="{{product.id}}" id="input-cart-order"/>
<!--                    <a href="/orders/new/{{product.id}}" class="btn btn-info active" role="button">주문</a>-->
                </div>
            </div>
        </div>
{{/shopLists}}
{{>layout/footer}}